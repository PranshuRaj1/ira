name = "ira-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[placement]
mode = "smart"

[assets]
directory = "./public"

[triggers]
crons = ["0 3 * * *"]   # Daily at 3 AM UTC — memory sleep cycle